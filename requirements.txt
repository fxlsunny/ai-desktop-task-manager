# ──────────────────────────────────────────────────────────
# AI Desktop Task Manager — Python dependencies
# ──────────────────────────────────────────────────────────
# 核心运行 = 0 依赖（纯标准库 + Tkinter）
# 下方均为「可选」，缺失时程序会优雅降级，不会崩溃
# ──────────────────────────────────────────────────────────

# 桌面截图 + 标注 + Markdown 预览插图（强烈建议）
pillow>=9.0.0

# 系统托盘图标（Windows / macOS / Linux 均支持）
pystray>=0.19.4

# 全局热键 Ctrl+Alt+A 截图（Windows 推荐；Linux 需 root；macOS 需辅助功能权限）
keyboard>=0.13.5 ; platform_system != "Darwin"
